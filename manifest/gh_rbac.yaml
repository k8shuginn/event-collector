---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: runner-clusterrole
rules:
  - apiGroups:
      - events.k8s.io
    resources:
      - events
    verbs: 
      - get
      - list
      - watch

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: runner-clusterrolebinding
subjects:
  - kind: ServiceAccount
    name: default
    namespace: gh-runner
roleRef:
  kind: ClusterRole
  name: runner-clusterrole
  apiGroup: rbac.authorization.k8s.io
